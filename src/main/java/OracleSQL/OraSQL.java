package OracleSQL;
/*
import Logger.Logger;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.SQLException;*/

public class OraSQL {
    /*
    public static boolean checkPassword(String pass, String user, String bisService){
    try {
        Logger.log("Start checking bis password",null);
        Class.forName("oracle.jdbc.driver.OracleDriver");
        Connection contest = DriverManager.getConnection("jdbc:oracle:thin:@msk-bis-db51.megafon.ru:1521/"+bisService+"", "ps_"+user, pass);
        contest.close();
        Logger.log("Password successfully checked",null);
        return  true;
    }
         catch(
    SQLException e){Logger.log(e.getMessage(),null);
        Logger.log("Connection with Bis didn't establish,bis password checked",null);  return false;}
         catch(ClassNotFoundException ex){Logger.log(ex.getMessage(),null);return false;}
         catch(Exception e){Logger.log(e.getMessage(),null); return false;}
}*/
}
